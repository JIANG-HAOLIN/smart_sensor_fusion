from dataclasses import dataclass
from typing import Any, Optional

# @dataclass
# class pl_trainer_conf:
#     # Configuration options defined here...
#     # Each attribute represents a configuration parameter.
#     # The default values are specified here as well.
#     pass
#
# @dataclass
# class pl_trainer_project_conf(pl_trainer_conf):
#     # This class inherits from LightningTrainerConf and adds/overrides some attributes.
#     # It's essentially a specialized configuration that inherits the base configuration.
#     pass
# # ConfigStore operations to register the defined configurations.
# # These configurations will be accessible via Hydra's configuration system.
