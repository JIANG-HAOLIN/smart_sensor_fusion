name: 'simple_transformer_pytorch'

model:
  _target_: src.models.trafo_predictor_pytorch.TransformerPredictor
  input_dim: 10
  model_dim: 32
  num_heads: 4
  num_classes: 10
  num_layers: 3
  dropout: 0.0


